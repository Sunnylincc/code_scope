# 当前限制

代码视界优先提供实用、紧凑、本地的结构化索引，因此需要理解以下限制：

- 静态分析可能不完整。当前解析器以轻量规则为主，不能覆盖所有语法细节。
- 动态调用难以完全识别。例如运行时拼接、反射、猴子补丁、依赖注入等模式可能无法准确建立关系。
- 跨语言关系暂时有限。不同语言之间的调用、生成代码和桥接层关系可能需要人工确认。
- 复杂框架的隐式依赖可能无法准确解析。约定式路由、装饰器、配置驱动绑定等场景可能只显示部分线索。
- 修改代码前仍然需要读取源文件确认。查询输出应作为导航和上下文压缩工具，而不是绝对事实来源。
